{
  "name": "london-visitor-compass",
  "version": "1.0.0",
  "private": true,
  "description": "Live London discovery dashboard",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "node scripts/server.mjs",
    "start": "npm run build && node scripts/server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
